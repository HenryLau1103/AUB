# Dashboard Overview — Implementation Brief

> **Version:** 0.3.0
> **Source:** this brief was generated by `scripts/export-md.mjs` from a UI Blueprint.
> **Target framework:** any (this brief is framework-agnostic)

This document is the Agent context export of a UI Blueprint. Read it top-to-bottom before implementing. The acceptance checklist at the bottom is the contract — every item must pass before this implementation is considered done.

---

## 1. Screen Summary

| Field | Value |
|---|---|
| **id** | `dashboard.overview` |
| **name** | Dashboard Overview |
| **type** | `dashboard` |
| **platform** | `web` |
| **primary_user_goal** | Let the user see high-level business metrics and drill into a specific data table row for detail. |
| **notes** | Reference example used to validate the schema, types, and exporters. |

## 2. Component Hierarchy

```
app_shell  (app_shell)
  sidebar  (sidebar)
    nav_item_overview  (nav_item)  [leaf]
    nav_item_orders  (nav_item)  [leaf]
    nav_item_customers  (nav_item)  [leaf]
    nav_item_reports  (nav_item)  [leaf]
    nav_item_settings  (nav_item)  [leaf]
  top_bar  (top_bar)
    search_input  (text_input)  [leaf]
    user_menu  (menu)  [leaf]
  main  (page)
    page_title  (section)  [leaf]
    metric_row  (section)
      metric_revenue  (metric_card)  [leaf]
      metric_orders  (metric_card)  [leaf]
      metric_customers  (metric_card)  [leaf]
      metric_conversion  (metric_card)  [leaf]
    chart_panel  (section)
      chart_placeholder  (chart_placeholder)  [leaf]
    recent_orders_table  (section)
      orders_table  (data_table)  [leaf]
      table_empty_state  (empty_state)  [leaf]
      table_loading_state  (loading_state)  [leaf]
      table_error_state  (error_state)  [leaf]
```

Every node in the tree is a **registered semantic component type** (not a generic rectangle). When you implement, you MUST use components that map to the stated type — not visual stand-ins.

---

## 3. Layout Contract

- **App Shell** (`app_shell`): mode: auto, display: grid, grid: 1 cols
- **Primary Sidebar** (`sidebar`): mode: auto, display: flex, direction: column, gap-y: 4px, padding: 24/16/24/16, width: 240px
- **Top Bar** (`top_bar`): mode: auto, display: flex, direction: row, gap-x: 16px, padding: 16/24/16/24
- **Main Content** (`main`): mode: auto, display: flex, direction: column, gap-y: 24px, padding: 24/32/32/32
- **Metric Row** (`metric_row`): mode: auto, display: grid, grid: 4 cols (1fr 1fr 1fr 1fr), gap: 16/16px
- **Revenue Chart** (`chart_panel`): mode: auto, display: block, padding: 16/16/16/16
- **Recent Orders** (`recent_orders_table`): mode: auto, display: flex, direction: column, gap-y: 8px

Use each container according to its declared mode: `auto` means flex/grid flow is the contract; `freeform` means child `placements` are the contract. Do not replace one mode with the other unless an acceptance item explicitly permits it.

### Design System Tokens

Token set: **AUB Neutral**

| Group | Token | Value |
|---|---|---|
| colors | `surface.canvas` | `#f8fafc` |
| colors | `surface.panel` | `#ffffff` |
| colors | `surface.subtle` | `#f1f5f9` |
| colors | `text.primary` | `#0f172a` |
| colors | `text.secondary` | `#64748b` |
| colors | `border.default` | `#cbd5e1` |
| colors | `action.primary` | `#2563eb` |
| colors | `action.primary.text` | `#ffffff` |
| colors | `status.success` | `#047857` |
| colors | `status.warning` | `#b45309` |
| colors | `status.danger` | `#b91c1c` |
| typography | `heading.page` | `700 28px/1.2 system-ui` |
| typography | `heading.section` | `650 18px/1.3 system-ui` |
| typography | `body.default` | `400 14px/1.5 system-ui` |
| typography | `body.small` | `400 12px/1.4 system-ui` |
| typography | `label.default` | `600 12px/1.3 system-ui` |
| spacing | `space.1` | `4px` |
| spacing | `space.2` | `8px` |
| spacing | `space.3` | `12px` |
| spacing | `space.4` | `16px` |
| spacing | `space.5` | `24px` |
| spacing | `space.6` | `32px` |
| radii | `radius.control` | `6px` |
| radii | `radius.panel` | `8px` |
| radii | `radius.round` | `999px` |
| shadows | `shadow.panel` | `0 8px 24px rgba(15, 23, 42, 0.12)` |
| shadows | `shadow.overlay` | `0 18px 48px rgba(15, 23, 42, 0.22)` |

---

## 4. Interaction Rules

| Source | Trigger | Action | Target | Result |
|---|---|---|---|---|
| `search_input` | `submit` | `navigate:/search` | — | User is routed to /search with the query string in the URL. |
| `user_menu` | `click` | `open:user_menu_dropdown` | — | Dropdown menu appears below the user menu with Profile and Sign out options. |
| `menu_logout` | `click` | `submit:logout` | — | User is signed out and redirected to /auth/login. |
| `orders_table` | `click` | `open:order_detail` | `order_detail` | A side drawer slides in from the right within 200ms showing the order detail panel. |
| `nav_item_overview` | `click` | `navigate:/dashboard` | — | User is routed to the dashboard overview screen. |
| `nav_item_settings` | `click` | `navigate:/settings` | — | User is routed to the settings screen. |

Every interaction declares its **trigger**, **action verb-noun**, and **observable result state**. Do not invent interactions not listed here.

---

## 5. Responsive Rules

| Viewport | Target | Rule | Effect |
|---|---|---|---|
| `tablet` | `sidebar` | `drawer` | defaultVisible: false, trigger: "hamburger" |
| `mobile` | `sidebar` | `drawer` | defaultVisible: false, trigger: "hamburger" |
| `tablet` | `metric_row` | `stack` | grid.columns: 2 |
| `mobile` | `metric_row` | `stack` | grid.columns: 1 |
| `mobile` | `orders_table` | `card_list` | view: "stacked", primary_field: "order_id" |

Every responsive decision is declared explicitly. The agent MUST NOT collapse the sidebar silently, hide the metric row, or change table to horizontal scroll without a declared rule.

---

## 6. Non-Violable Conditions

1. **App shell uses a grid layout with sidebar (240px), top bar, and main content area.** _(target: `app_shell`)_

---

## 7. Acceptance Checklist

This checklist is the contract. Every item must be verifiable. Mark each `[x]` only after manual or automated verification.

### Layout (2)

- [ ] **acc_layout_shell** (blocker) — App shell uses a grid layout with sidebar (240px), top bar, and main content area. _[verify: `computed_style`]_
- [ ] **acc_layout_metric_row** (must) — Metric row displays 4 cards in a 4-column grid on desktop with 16px gap. _[verify: `computed_style`]_

### Interaction (2)

- [ ] **acc_interaction_row_click** (must) — Clicking an order row opens a detail drawer within 200ms. _[verify: `interaction_replay`]_
- [ ] **acc_interaction_logout** (must) — Clicking Sign out terminates the session and routes to /auth/login. _[verify: `interaction_replay`]_

### Responsive (3)

- [ ] **acc_responsive_sidebar** (must) — Sidebar converts to a drawer triggered by a hamburger icon below 1024px viewport width. _[verify: `manual_visual`]_
- [ ] **acc_responsive_metric_row** (must) — Metric row collapses to 2 columns at tablet (1024px) and 1 column at mobile (390px). _[verify: `screenshot_diff`]_
- [ ] **acc_responsive_table** (must) — Orders table converts to a card list on mobile (390px) instead of horizontal scroll. _[verify: `screenshot_diff`]_

### Accessibility (3)

- [ ] **acc_a11y_keyboard** (must) — All interactive elements are reachable by keyboard tab order and show a visible focus ring. _[verify: `axe_audit`]_
- [ ] **acc_a11y_labels** (must) — All form inputs have an associated label or aria-label. _[verify: `axe_audit`]_
- [ ] **acc_a11y_touch_targets** (should) — All interactive elements have a minimum touch target of 44x44 pixels on mobile. _[verify: `computed_style`]_

### Content (1)

- [ ] **acc_content_empty** (should) — When the orders list is empty, the empty_state node is rendered instead of an empty table. _[verify: `dom_query`]_

**Total: 11 verifiable items** (≥5 required by spec).

---

## 8. Agent Task

You are implementing the Dashboard Overview described above. Your deliverable is:

1. A new file or route matching the component hierarchy in §2.
2. Implementation that respects the layout contract in §3.
3. Wiring of all interactions in §4.
4. Responsive behavior per §5.
5. A passing run of the acceptance checklist in §7 — every item verifiable, not vibes.

### Implementation order (suggested, not strict)

1. App shell + main containers (skeleton, no data)
2. Children nodes with hardcoded content
3. Wire primary interactions
4. Wire secondary interactions and edge cases
5. Add responsive rules
6. Add empty/loading/error states
7. Run accessibility audit, fix any issues
8. Run acceptance checklist, mark all items

### Stop conditions

- **Blocker found:** stop and report. Do not "fix forward" by relaxing the spec.
- **Acceptance item impossible as specified:** stop and report. Do not silently weaken the criterion.
- **Component type does not exist in your framework:** stop and report. Do not substitute a generic `<div>`.

### Output format (when done)

Report per acceptance item:

```
[ ] acc_id — pass | fail | needs-review
    evidence: <screenshot path | file:line | dom query | interaction log>
    notes: <if any>
```

If any item is `fail` or `needs-review`, the implementation is not done.
