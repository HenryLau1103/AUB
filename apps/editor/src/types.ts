// Re-exports from the root schema so the editor imports from one place.
export type {
  Blueprint,
  Screen,
  Viewport,
  UINode,
  Layout,
  Content,
  Interaction,
  Responsive,
  Acceptance,
  ComponentType,
} from '../../../schema/types';
